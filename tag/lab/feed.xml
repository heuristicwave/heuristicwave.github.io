<?xml version="1.0" encoding="utf-8"?>

<feed xmlns="http://www.w3.org/2005/Atom" >
  <generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator>
  <link href="https://heuristicwave.github.io/tag/lab/feed.xml" rel="self" type="application/atom+xml" />
  <link href="https://heuristicwave.github.io/" rel="alternate" type="text/html" />
  <updated>2026-06-25T16:30:11+00:00</updated>
  <id>https://heuristicwave.github.io/tag/lab/feed.xml</id>

  
  
  

  
    <title type="html">Heuristic Wave Blog | </title>
  

  
    <subtitle>Careful Writer</subtitle>
  

  

  
    
      
    
  

  
  

  
    <entry>
      <title type="html">Amazon S3 Annotations 2편: 객체에 의미 계층 붙이기</title>
      <link href="https://heuristicwave.github.io/S3-Annotations-2" rel="alternate" type="text/html" title="Amazon S3 Annotations 2편: 객체에 의미 계층 붙이기" />
      <published>2026-06-25T00:00:00+00:00</published>
      <updated>2026-06-25T00:00:00+00:00</updated>
      <id>https://heuristicwave.github.io/S3-Annotations-2</id>
      <content type="html" xml:base="https://heuristicwave.github.io/S3-Annotations-2">&lt;p&gt;이번 글에서는 S3 Annotations를 특정 예제의 부가 정보 저장소가 아니라, S3 객체 전반에 의미 계층을 붙이는 context layer로 보는 관점을 정리합니다.&lt;/p&gt;

&lt;p&gt;&lt;a href=&quot;/S3-OKF-Annotations.html&quot;&gt;1편&lt;/a&gt;에서는 S3 Annotations로 Open Knowledge Format을 흉내내는 실험을 했습니다.
Markdown 문서를 S3 Object로 올리고, YAML frontmatter, Markdown links, summary를 각각 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.frontmatter&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.links&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.summary&lt;/code&gt; annotation으로 분리했습니다.&lt;/p&gt;

&lt;p&gt;다만 S3 bucket에 들어가는 것은 Markdown 문서만이 아닙니다.
이미지, 오디오, PDF, 로그, Parquet, 모델 산출물, 임베딩 참조, 거버넌스 정책까지 모두 객체로 존재할 수 있습니다.
이 객체들을 다루기 시작하면 S3 Annotations는 “특정 문서 포맷의 metadata 저장소”라기보다 &lt;strong&gt;S3 객체에 의미 계층을 붙이는 context layer&lt;/strong&gt;에 가까워집니다.&lt;/p&gt;

&lt;p&gt;여기서 자연스럽게 S3 Metadata tables와의 관계도 다시 봐야 합니다.
S3 Metadata tables가 객체의 상태와 변경 이력을 자동으로 테이블화한다면, S3 Annotations는 사람이 또는 시스템이 직접 붙이는 의미를 객체 옆에 둡니다.
그리고 S3 Metadata annotation table은 그 의미를 Athena 같은 분석 엔진에서 조회할 수 있게 만드는 인덱스 역할을 합니다.&lt;/p&gt;

&lt;p&gt;이 관점에서 보면 이번 글의 핵심은 두 가지입니다.&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;S3 Metadata tables는 객체 상태와 변경 이력을 자동으로 테이블화하고, S3 Annotations는 객체에 사람이 또는 시스템이 직접 의미를 붙입니다.&lt;/li&gt;
  &lt;li&gt;Markdown 문서를 넘어 이미지, 오디오, PDF, 데이터셋까지 다루려면 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;asset.*&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;analysis.*&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;agent.*&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;governance.*&lt;/code&gt; 같은 역할별 namespace가 필요합니다.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;&lt;br /&gt;&lt;/p&gt;

&lt;h2 id=&quot;-s3-metadata와-s3-annotations는-무엇이-다른가&quot;&gt;&lt;a href=&quot;#metadata-vs-annotations&quot;&gt;🤔 S3 Metadata와 S3 Annotations는 무엇이 다른가&lt;/a&gt;&lt;a id=&quot;metadata-vs-annotations&quot;&gt;&lt;/a&gt;&lt;/h2&gt;

&lt;p&gt;S3 Annotations를 정확히 이해하려면 먼저 S3에서 말하는 metadata, Metadata tables, annotation table의 범위를 잡고 가야 합니다.
세 용어가 모두 “객체를 설명하는 정보”처럼 보이지만, 실제로는 붙는 위치와 조회 방식이 다릅니다.&lt;/p&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;strong&gt;&lt;a href=&quot;https://docs.aws.amazon.com/AmazonS3/latest/userguide/UsingMetadata.html&quot;&gt;S3 object metadata&lt;/a&gt;&lt;/strong&gt;는 객체 자체에 붙어 있는 system-defined metadata와 user-defined metadata입니다.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;&lt;a href=&quot;https://docs.aws.amazon.com/AmazonS3/latest/userguide/metadata-tables-overview.html&quot;&gt;S3 Metadata tables&lt;/a&gt;&lt;/strong&gt;는 그 객체 metadata와 변경 이벤트를 read-only Apache Iceberg table로 자동 수집해 조회하게 해주는 기능입니다.&lt;/li&gt;
  &lt;li&gt;&lt;strong&gt;&lt;a href=&quot;https://docs.aws.amazon.com/AmazonS3/latest/userguide/annotations-overview.html&quot;&gt;S3 Annotations&lt;/a&gt;&lt;/strong&gt;는 객체를 다시 업로드하지 않고도 객체 버전 옆에 named payload를 따로 붙이는 기능입니다.&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;&lt;img src=&quot;/assets/built/images/post/ai/s3-annotations/metadata-vs-annotations.svg&quot; alt=&quot;S3 Metadata tables와 S3 Annotations 관계&quot; /&gt;&lt;/p&gt;

&lt;p&gt;위 그림에서 중요한 점은 S3 Annotations가 S3 object metadata 안에 들어가는 필드가 아니라는 점입니다.
Annotations는 객체 버전 옆에 따로 붙고, S3 Metadata tables의 annotation table을 통해 조회할 수 있습니다.&lt;/p&gt;

&lt;p&gt;공식 문서 기준으로 S3 Metadata tables는 기본적으로 system-defined object metadata, object tag, upload 시점의 user-defined metadata를 수집하고, 객체가 언제 수정되거나 삭제되었는지 같은 event metadata도 제공합니다.
여기서 말하는 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;user_metadata&lt;/code&gt;는 실제 schema에 있는 컬럼이고, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;requester&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;source_ip_address&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;request_id&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;record_type&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;record_timestamp&lt;/code&gt; 같은 값이 event 쪽을 설명합니다.&lt;/p&gt;

&lt;p&gt;표로 비교하면 다음과 같습니다.&lt;/p&gt;

&lt;table style=&quot;display:table; width:100%; table-layout:fixed; overflow-x:visible; white-space:normal; overflow-wrap:anywhere;&quot;&gt;
  &lt;colgroup&gt;
    &lt;col style=&quot;width:18%;&quot; /&gt;
    &lt;col style=&quot;width:41%;&quot; /&gt;
    &lt;col style=&quot;width:41%;&quot; /&gt;
  &lt;/colgroup&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th&gt;구분&lt;/th&gt;
      &lt;th&gt;S3 Metadata tables&lt;/th&gt;
      &lt;th&gt;S3 Annotations&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td&gt;본질&lt;/td&gt;
      &lt;td&gt;S3 객체 상태와 변경 이벤트를 queryable table로 제공&lt;/td&gt;
      &lt;td&gt;객체 버전에 named payload를 붙임&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;생성 주체&lt;/td&gt;
      &lt;td&gt;Amazon S3가 객체와 이벤트에서 자동 수집&lt;/td&gt;
      &lt;td&gt;애플리케이션, Agent, ML pipeline, 운영 시스템&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;수정 방식&lt;/td&gt;
      &lt;td&gt;Metadata table 자체는 read-only&lt;/td&gt;
      &lt;td&gt;전용 annotation API로 생성/수정/삭제&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;대표 데이터&lt;/td&gt;
      &lt;td&gt;object key, size, storage class&lt;br /&gt;tag, user metadata&lt;br /&gt;event type, requester&lt;/td&gt;
      &lt;td&gt;label, transcript, lineage&lt;br /&gt;processing result&lt;br /&gt;compliance record&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;조회 테이블&lt;/td&gt;
      &lt;td&gt;journal table&lt;br /&gt;live inventory table&lt;br /&gt;annotation table&lt;/td&gt;
      &lt;td&gt;S3 Metadata annotation table&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;답하는 질문&lt;/td&gt;
      &lt;td&gt;&quot;버킷에 무엇이 있고 어떻게 변했나?&quot;&lt;/td&gt;
      &lt;td&gt;&quot;이 객체를 어떤 맥락으로 읽어야 하나?&quot;&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;S3 Annotations는 S3 object metadata의 하위 필드가 아니라, 객체 옆에 붙는 별도 context payload입니다.
User-defined metadata는 업로드 시점의 header에 가깝고, 바꾸려면 객체 copy가 필요하지만, annotation은 별도 API로 붙이고 S3 Metadata annotation table을 통해 조회할 수 있습니다.&lt;/p&gt;

&lt;p&gt;예를 들어 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;assets/audio/music.m4a&lt;/code&gt;라는 객체가 있다고 해보겠습니다.
S3 Metadata tables는 이 객체의 상태를 잘 설명합니다.&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;key = assets/audio/music.m4a
size = 8MB
storage_class = STANDARD
encryption_status = SSE-S3
last_modified_date = ...
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;하지만 객체를 실제로 활용하려면 상태만으로는 부족합니다.
다음은 객체의 의미와 사용 맥락에 가까운 질문입니다.&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;이 오디오 파일은 어떤 캠페인과 연결되는가?
이 파일의 transcript는 어디에 있는가?
이 asset은 어떤 OKF 문서가 설명하는가?
Agent가 이 파일을 사용할 때 어떤 retrieval hint를 따라야 하는가?
이 파일은 내부 전용인가, 외부 공유 가능한가?
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;이런 맥락은 S3 Annotations로 객체 옆에 붙이는 편이 자연스럽습니다.&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;asset.media
analysis.labels
analysis.transcript
analysis.embedding_ref
governance.policy
context.relations
agent.retrieval_hint
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;지금까지의 관계를 짧게 정리하면 다음과 같습니다.&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;S3 Metadata tables
  ├─ journal table        # 객체 변경 이벤트
  ├─ live inventory table # 현재 객체 상태
  └─ annotation table     # S3 Annotations payload를 queryable하게 만든 테이블
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;즉 S3 Annotations가 S3 Metadata tables를 대체하는 것은 아닙니다.
S3 Annotations는 객체 옆에 context를 붙이는 기능이고, S3 Metadata annotation table은 그 context를 대량 조회하게 해주는 테이블입니다.
여기까지 정리하고 나면 다음 문제는 자연스럽게 “annotation 이름을 어떻게 나눌 것인가”가 됩니다.&lt;/p&gt;

&lt;p&gt;&lt;br /&gt;&lt;/p&gt;

&lt;h2 id=&quot;-context-layer-만들기&quot;&gt;&lt;a href=&quot;#namespace&quot;&gt;🧩 Context Layer 만들기&lt;/a&gt;&lt;a id=&quot;namespace&quot;&gt;&lt;/a&gt;&lt;/h2&gt;

&lt;p&gt;&lt;a href=&quot;https://aws.amazon.com/ko/blogs/aws/amazon-s3-annotations-attach-rich-queryable-context-directly-to-your-objects/&quot;&gt;AWS의 S3 Annotations 소개 글&lt;/a&gt;은 S3 객체 옆에 rich, queryable context를 붙일 수 있다는 방향을 보여줍니다.
다만 그 글이 의미 계층을 어떻게 나눌지에 대한 Best Practice나 청사진까지 제시하는 것은 아닙니다.
그래서 여기서는 S3 Annotations로 의미 계층을 만든다면 namespace를 어떻게 나눌 수 있을지 작게 시도해보겠습니다.&lt;/p&gt;

&lt;p&gt;S3 버킷에는 Markdown 문서만 있는 것이 아닙니다.
이미지, 오디오, PDF, Parquet, 모델 산출물처럼 서로 다른 성격의 객체가 함께 존재할 수 있습니다.
이 객체들에 붙는 annotation까지 하나의 context layer로 다루려면, annotation 이름은 파일 형식보다 payload의 책임을 더 잘 드러내야 합니다.&lt;/p&gt;

&lt;p&gt;그래서 이 글에서는 namespace를 “어떤 포맷에서 왔는가”보다 “무엇을 설명하는가” 기준으로 나눠보겠습니다.&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;okf.*          # OKF 문서 구조
asset.*        # 파일/미디어 자체 정보
analysis.*     # 이미지/오디오/ML 분석 결과
governance.*   # 민감도, 보존, 접근 정책
agent.*        # Agent 검색/라우팅 단서
context.*      # 객체 간 관계
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;각 namespace의 책임은 다음처럼 생각할 수 있습니다.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th&gt;Namespace&lt;/th&gt;
      &lt;th&gt;역할&lt;/th&gt;
      &lt;th&gt;예시&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.*&lt;/code&gt;&lt;/td&gt;
      &lt;td&gt;OKF 문서 포맷&lt;/td&gt;
      &lt;td&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.frontmatter&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.summary&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.links&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;asset.*&lt;/code&gt;&lt;/td&gt;
      &lt;td&gt;객체 자체의 설명&lt;/td&gt;
      &lt;td&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;asset.media&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;asset.caption&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;analysis.*&lt;/code&gt;&lt;/td&gt;
      &lt;td&gt;분석 결과&lt;/td&gt;
      &lt;td&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;analysis.labels&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;analysis.transcript&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;analysis.embedding_ref&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;governance.*&lt;/code&gt;&lt;/td&gt;
      &lt;td&gt;정책과 통제&lt;/td&gt;
      &lt;td&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;governance.policy&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;governance.retention&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;agent.*&lt;/code&gt;&lt;/td&gt;
      &lt;td&gt;Agent 검색/라우팅 단서&lt;/td&gt;
      &lt;td&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;agent.summary&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;agent.retrieval_hint&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;agent.routing&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;context.*&lt;/code&gt;&lt;/td&gt;
      &lt;td&gt;객체 간 관계&lt;/td&gt;
      &lt;td&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;context.relations&lt;/code&gt;&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;이렇게 나누면 S3 bucket 안의 다양한 객체를 하나의 의미 그래프로 묶을 수 있습니다.&lt;/p&gt;

&lt;p&gt;여기서 중요한 것은 namespace를 “누가 읽을 것인가”가 아니라 “무엇을 책임지는가” 기준으로 나누는 것입니다.
예를 들어 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;agent.summary&lt;/code&gt;가 편하다고 해서 민감도나 보존 정책까지 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;agent.*&lt;/code&gt;에 넣기 시작하면, Agent 검색 단서와 governance policy가 섞입니다.
반대로 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;governance.*&lt;/code&gt;는 정책과 통제만 담당하고, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;agent.*&lt;/code&gt;는 Agent가 참고할 단서만 담당하게 하면 같은 객체를 여러 도구가 읽어도 역할이 덜 꼬입니다.
이제 이 namespace 중에서 관계를 다루는 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;context.relations&lt;/code&gt;를 조금 더 자세히 보겠습니다.&lt;/p&gt;

&lt;p&gt;&lt;br /&gt;&lt;/p&gt;

&lt;h2 id=&quot;-contextrelations로-관계-만들기&quot;&gt;&lt;a href=&quot;#relations&quot;&gt;🔗 context.relations로 관계 만들기&lt;/a&gt;&lt;a id=&quot;relations&quot;&gt;&lt;/a&gt;&lt;/h2&gt;

&lt;p&gt;Context layer를 만들 때 중요한 것은 개별 객체의 설명만 붙이는 것이 아닙니다.
객체와 객체 사이의 관계도 함께 표현해야 합니다.
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;context.relations&lt;/code&gt;는 이 역할을 맡는 중립적인 relation layer로 둘 수 있습니다.&lt;/p&gt;

&lt;p&gt;이 관계는 Markdown 문서만 대상으로 하지 않습니다.
이미지, 오디오, PDF, 데이터셋, 모델 산출물처럼 S3에 있는 어떤 객체도 연결 대상이 될 수 있습니다.&lt;/p&gt;

&lt;p&gt;예를 들어 다음 두 객체가 있다고 가정합니다.&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;okf/music/campaign-theme.md
assets/audio/music.m4a
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;이 둘은 본문 포맷은 다르지만, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;context.relations&lt;/code&gt;로 link 관계를 맺을 수 있습니다.&lt;/p&gt;

&lt;div class=&quot;language-json highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
  &lt;/span&gt;&lt;span class=&quot;nl&quot;&gt;&quot;relations&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;w&quot;&gt; &lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
    &lt;/span&gt;&lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
      &lt;/span&gt;&lt;span class=&quot;nl&quot;&gt;&quot;type&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;w&quot;&gt; &lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;describes&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
      &lt;/span&gt;&lt;span class=&quot;nl&quot;&gt;&quot;target&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;w&quot;&gt; &lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;assets/audio/music.m4a&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
      &lt;/span&gt;&lt;span class=&quot;nl&quot;&gt;&quot;target_kind&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;w&quot;&gt; &lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;asset&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
      &lt;/span&gt;&lt;span class=&quot;nl&quot;&gt;&quot;label&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;w&quot;&gt; &lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;campaign theme audio&quot;&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
    &lt;/span&gt;&lt;span class=&quot;p&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
  &lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;필요하면 반대쪽 객체에는 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;described_by&lt;/code&gt; 같은 역방향 relation을 둘 수 있습니다.
핵심은 두 객체가 같은 파일 형식일 필요가 없다는 점입니다.&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;okf/music/campaign-theme.md
  -- context.relations: describes --&amp;gt;
assets/audio/music.m4a

assets/audio/music.m4a
  -- context.relations: described_by --&amp;gt;
okf/music/campaign-theme.md
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;즉 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;context.relations&lt;/code&gt;는 특정 포맷의 링크 추출 결과가 아니라, S3 객체 전체를 연결하는 context graph의 가장 작은 단위입니다.&lt;/p&gt;

&lt;p&gt;&lt;br /&gt;&lt;/p&gt;

&lt;h2 id=&quot;-어디에-쓸-수-있을까&quot;&gt;&lt;a href=&quot;#use-cases&quot;&gt;🧭 어디에 쓸 수 있을까&lt;/a&gt;&lt;a id=&quot;use-cases&quot;&gt;&lt;/a&gt;&lt;/h2&gt;

&lt;p&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;context.relations&lt;/code&gt;는 여러 포맷의 객체를 하나의 주제로 묶을 때 가장 이해하기 쉽습니다.
예를 들어 월드컵 캠페인 관련 자료가 S3에 흩어져 있다고 해보겠습니다.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;/assets/built/images/post/ai/s3-annotations/worldcup-context-relations.svg&quot; alt=&quot;World Cup context relations&quot; style=&quot;display:block; width:88%; max-width:920px; margin:1.5em auto;&quot; /&gt;&lt;/p&gt;

&lt;p&gt;각 객체의 본문은 Markdown, 영상, 이미지, 오디오, Parquet처럼 서로 다릅니다.
하지만 사용자나 Agent 입장에서는 모두 “월드컵 캠페인”이라는 같은 맥락의 자료입니다.
이때 캠페인 overview 문서에 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;context.relations&lt;/code&gt;를 붙여 관련 객체를 묶을 수 있습니다.&lt;/p&gt;

&lt;p&gt;이렇게 해두면 원본 파일을 한 곳으로 복사하지 않아도 됩니다.
각 객체는 자기 위치에 그대로 두고, 관계만 annotation으로 붙입니다.
검색 화면이나 Agent는 먼저 annotation table에서 월드컵 overview 문서를 찾고, 그 문서의 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;context.relations&lt;/code&gt;를 펼쳐 관련 영상, 이미지, 음원, 데이터셋을 함께 보여줄 수 있습니다.&lt;/p&gt;

&lt;p&gt;이 패턴의 핵심은 “객체를 옮기지 않고 의미 계층만 얹는다”는 점입니다.
Media Asset 관리에서는 캠페인 문서, 원본 영상, 썸네일 이미지, 배경 음원, 자막 파일을 같은 asset group으로 묶을 수 있습니다.
검색 화면은 객체 본문을 모두 열지 않아도 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;context.relations&lt;/code&gt;를 따라가며 관련 자료를 함께 보여줄 수 있습니다.&lt;/p&gt;

&lt;p&gt;AI SRE 쪽으로 옮기면 같은 패턴을 로그와 운영 문서에도 적용할 수 있습니다.
예를 들어 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;incidents/payment-5xx.md&lt;/code&gt;가 특정 시간대의 log parquet, metric export, deployment event, runbook을 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;context.relations&lt;/code&gt;로 참조하게 만들 수 있습니다.
그러면 Agent는 raw log를 처음부터 훑기보다, 먼저 incident summary가 연결한 telemetry와 runbook을 따라가며 조사 범위를 좁힐 수 있습니다.&lt;/p&gt;

&lt;p&gt;중요한 점은 이미지, 오디오, 로그 원문을 annotation payload에 넣는 것이 아닙니다.
S3 object는 그대로 두고, annotation에는 “이 객체가 무엇과 연결되는가”라는 얇은 의미 계층만 둡니다.
객체의 storage class나 size는 S3 Metadata tables가 알려주고, 이 객체가 어떤 캠페인, 문서, 로그, 정책과 연결되는지는 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;context.relations&lt;/code&gt;가 설명합니다.
여기까지가 의미 계층을 만드는 방식이라면, 운영에서는 곧바로 보안 문제가 따라옵니다.&lt;/p&gt;

&lt;p&gt;&lt;br /&gt;&lt;/p&gt;

&lt;h2 id=&quot;-객체-권한과-annotation-권한을-나눠서-보기&quot;&gt;&lt;a href=&quot;#permissions&quot;&gt;🔐 객체 권한과 annotation 권한을 나눠서 보기&lt;/a&gt;&lt;a id=&quot;permissions&quot;&gt;&lt;/a&gt;&lt;/h2&gt;

&lt;p&gt;의미 계층은 얇지만, 운영 시스템에서는 별도의 데이터 노출면입니다.
Agent나 검색/카탈로그 화면이 annotation을 읽으면 원본 객체를 열지 않아도 관계, 요약, 정책 단서가 드러날 수 있습니다.
그래서 객체 본문 권한과 annotation 권한은 분리해서 봐야 합니다.&lt;/p&gt;

&lt;p&gt;객체 본문은 기존처럼 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PutObject&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;GetObject&lt;/code&gt;로 다룹니다.
Annotation은 별도 API로 생성, 조회, 삭제합니다.&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;PutObjectAnnotation
GetObjectAnnotation
ListObjectAnnotations
DeleteObjectAnnotation
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;이 차이는 권한 설계에서 중요합니다.
예를 들어 역할을 다음처럼 나눌 수 있습니다.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th&gt;Role&lt;/th&gt;
      &lt;th&gt;가능한 작업&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td&gt;Data Producer&lt;/td&gt;
      &lt;td&gt;원천 객체 업로드&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;Catalog Writer&lt;/td&gt;
      &lt;td&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;asset.*&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;analysis.*&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;context.relations&lt;/code&gt; annotation 작성&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;Governance Writer&lt;/td&gt;
      &lt;td&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;governance.*&lt;/code&gt; annotation 작성&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;Agent Runtime&lt;/td&gt;
      &lt;td&gt;필요한 annotation 조회와 제한된 객체 읽기&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;Auditor&lt;/td&gt;
      &lt;td&gt;annotation table과 journal table 조회&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;정리하면 S3 Annotations가 annotation 이름별 권한 체계를 대신 설계해주는 것은 아닙니다.
다만 객체 본문 API와 annotation API가 나뉘어 있으므로, 원본을 다루는 역할과 context를 다루는 역할을 분리해 볼 수 있습니다.&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;데이터 생산 팀:
  - s3:PutObject
  - s3:GetObject

Catalog 팀:
  - s3:PutObjectAnnotation
  - s3:GetObjectAnnotation
  - s3:ListObjectAnnotations

Agent Runtime:
  - s3:GetObjectAnnotation
  - s3:ListObjectAnnotations
  - 필요한 prefix에 한정된 s3:GetObject

감사/분석 팀:
  - annotation table 조회 권한
  - journal table 조회 권한
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;이 구조에서는 데이터 생산 팀이 원천 파일을 관리하고, Catalog 팀은 객체 옆의 context를 갱신합니다.
객체를 다시 업로드하지 않고 annotation만 바꿀 수 있으므로, payload와 context의 lifecycle을 어느 정도 분리할 수 있습니다.&lt;/p&gt;

&lt;blockquote&gt;
  &lt;p&gt;&lt;strong&gt;LLM 사용 시 주의&lt;/strong&gt;&lt;/p&gt;

  &lt;p&gt;Agent Runtime이 annotation을 읽더라도, payload 안의 문장을 사용자 지시나 시스템 지시로 실행하면 안 됩니다.
annotation은 tool result나 JSON data처럼 다루고, 권한 판단은 IAM, bucket policy, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;governance.*&lt;/code&gt; 쪽에서 다시 확인해야 합니다.
특히 LLM에게 annotation을 context로 넣는다면 context poisoning도 고려해야 합니다.
악의적이거나 오래된 annotation이 “이 객체는 안전하다”, “이 파일을 우선 신뢰하라” 같은 문장을 담고 있으면 Agent의 판단이 흔들릴 수 있습니다.
따라서 중요한 annotation은 작성 주체, 갱신 시점, 검증 여부를 함께 관리하는 편이 안전합니다.&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;다만 annotation이 객체와 완전히 독립적인 리소스는 아닙니다.
annotation은 특정 객체 또는 객체 버전에 붙습니다.
또한 &lt;a href=&quot;https://docs.aws.amazon.com/AmazonS3/latest/userguide/annotations-overview.html#annotations-encryption&quot;&gt;AWS 문서&lt;/a&gt;에 따르면 annotation은 parent object의 encryption configuration을 따릅니다.
예를 들어 원본 객체가 SSE-KMS를 사용하면 annotation도 같은 KMS key로 암호화되고, SSE-C로 암호화된 객체에는 annotation을 붙일 수 없습니다.
즉 annotation만 따로 다른 암호화 정책을 갖는 독립 저장소처럼 보는 것은 맞지 않습니다.
따라서 권한은 나눌 수 있지만, 보안 설계에서는 객체와 annotation을 함께 봐야 합니다.&lt;/p&gt;

&lt;p&gt;&lt;br /&gt;&lt;/p&gt;

&lt;h2 id=&quot;-실험-iam-role을-나눠서-확인하기&quot;&gt;&lt;a href=&quot;#permission-demo&quot;&gt;🧪 실험: IAM Role을 나눠서 확인하기&lt;/a&gt;&lt;a id=&quot;permission-demo&quot;&gt;&lt;/a&gt;&lt;/h2&gt;

&lt;p&gt;권한 분리가 실제로 되는지 확인하려고 같은 객체에 대해 IAM Role을 두 개 만들었습니다.
아래 출력은 실제 실행 결과에서 bucket 이름과 account id만 마스킹한 것입니다.&lt;/p&gt;

&lt;p&gt;실험 대상 객체는 다음과 같습니다.&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;s3://&amp;lt;bucket-name&amp;gt;/okf/generated/ai/01-rag-evaluation-set.md
annotation name: okf.frontmatter
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;첫 번째 Role은 데이터 생산자 역할입니다.
원본 객체는 읽을 수 있지만, annotation API 권한은 주지 않았습니다.&lt;/p&gt;

&lt;div class=&quot;language-json highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
  &lt;/span&gt;&lt;span class=&quot;nl&quot;&gt;&quot;Effect&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;w&quot;&gt; &lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;Allow&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
  &lt;/span&gt;&lt;span class=&quot;nl&quot;&gt;&quot;Action&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;w&quot;&gt; &lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;s3:GetObject&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;w&quot;&gt; &lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;s3:PutObject&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;],&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
  &lt;/span&gt;&lt;span class=&quot;nl&quot;&gt;&quot;Resource&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;w&quot;&gt; &lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;arn:aws:s3:::&amp;lt;bucket-name&amp;gt;/okf/generated/ai/01-rag-evaluation-set.md&quot;&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;이 Role로 assume-role한 뒤 실행한 결과입니다.&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;## DataProducer role
$ aws s3api get-object ... /tmp/object.md
GetObject: SUCCESS (757 bytes)

$ GetObjectAnnotation okf.frontmatter
AccessDenied: no identity-based policy allows the s3:GetObjectAnnotation action
HTTP_STATUS:403
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;두 번째 Role은 Catalog Reader 역할입니다.
반대로 원본 객체 읽기는 명시적으로 막고, annotation 조회만 허용했습니다.&lt;/p&gt;

&lt;div class=&quot;language-json highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
  &lt;/span&gt;&lt;span class=&quot;nl&quot;&gt;&quot;Version&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;w&quot;&gt; &lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;2012-10-17&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
  &lt;/span&gt;&lt;span class=&quot;nl&quot;&gt;&quot;Statement&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;w&quot;&gt; &lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
    &lt;/span&gt;&lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
      &lt;/span&gt;&lt;span class=&quot;nl&quot;&gt;&quot;Effect&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;w&quot;&gt; &lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;Deny&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
      &lt;/span&gt;&lt;span class=&quot;nl&quot;&gt;&quot;Action&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;w&quot;&gt; &lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;s3:GetObject&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
      &lt;/span&gt;&lt;span class=&quot;nl&quot;&gt;&quot;Resource&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;w&quot;&gt; &lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;arn:aws:s3:::&amp;lt;bucket-name&amp;gt;/okf/generated/ai/01-rag-evaluation-set.md&quot;&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
    &lt;/span&gt;&lt;span class=&quot;p&quot;&gt;},&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
    &lt;/span&gt;&lt;span class=&quot;p&quot;&gt;{&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
      &lt;/span&gt;&lt;span class=&quot;nl&quot;&gt;&quot;Effect&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;w&quot;&gt; &lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;Allow&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
      &lt;/span&gt;&lt;span class=&quot;nl&quot;&gt;&quot;Action&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;w&quot;&gt; &lt;/span&gt;&lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
        &lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;s3:GetObjectAnnotation&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
        &lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;s3:ListObjectAnnotations&quot;&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
      &lt;/span&gt;&lt;span class=&quot;p&quot;&gt;],&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
      &lt;/span&gt;&lt;span class=&quot;nl&quot;&gt;&quot;Resource&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;:&lt;/span&gt;&lt;span class=&quot;w&quot;&gt; &lt;/span&gt;&lt;span class=&quot;s2&quot;&gt;&quot;arn:aws:s3:::&amp;lt;bucket-name&amp;gt;/okf/generated/ai/01-rag-evaluation-set.md&quot;&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
    &lt;/span&gt;&lt;span class=&quot;p&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
  &lt;/span&gt;&lt;span class=&quot;p&quot;&gt;]&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;}&lt;/span&gt;&lt;span class=&quot;w&quot;&gt;
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;이 Role로 assume-role한 결과는 반대로 나왔습니다.&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;## CatalogReader role
$ aws s3api get-object ... /tmp/object.md
AccessDenied: explicit deny in an identity-based policy

$ GetObjectAnnotation okf.frontmatter
{
  &quot;type&quot;: &quot;Context&quot;,
  &quot;title&quot;: &quot;RAG Evaluation Set&quot;,
  &quot;description&quot;: &quot;A small benchmark for retrieval quality and answer grounding.&quot;,
  &quot;domain&quot;: &quot;ai&quot;,
  &quot;language&quot;: &quot;en&quot;,
  &quot;owner&quot;: &quot;ai-context-team&quot;,
  &quot;resource&quot;: &quot;context://ai/rag-evaluation-set&quot;,
  &quot;tags&quot;: [&quot;ai&quot;, &quot;en&quot;, &quot;context&quot;],
  &quot;status&quot;: &quot;experimental&quot;,
  &quot;sensitivity&quot;: &quot;internal&quot;,
  &quot;timestamp&quot;: &quot;2026-06-24T00:00:00Z&quot;
}
HTTP_STATUS:200
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;이 결과가 보여주는 것은 명확합니다.
원본 객체를 읽을 수 있는 권한과 annotation을 읽을 수 있는 권한은 같은 것이 아닙니다.
Data Producer는 객체 본문을 읽었지만 annotation은 읽지 못했고, Catalog Reader는 객체 본문을 읽지 못했지만 annotation payload는 읽었습니다.&lt;/p&gt;

&lt;p&gt;따라서 annotation은 단순한 보조 필드가 아니라 별도의 데이터 노출면으로 봐야 합니다.
Agent나 검색/카탈로그 화면에 annotation 조회 권한을 줄 때는, 원본 객체 권한과 별개로 어떤 annotation payload가 노출되는지 검토해야 합니다.
민감한 원문은 객체에 두고, annotation에는 검색과 판단에 필요한 최소한의 context만 넣는 편이 안전합니다.&lt;/p&gt;

&lt;p&gt;&lt;br /&gt;&lt;/p&gt;

&lt;h2 id=&quot;️-주의할-점&quot;&gt;&lt;a href=&quot;#limits&quot;&gt;⚠️ 주의할 점&lt;/a&gt;&lt;a id=&quot;limits&quot;&gt;&lt;/a&gt;&lt;/h2&gt;

&lt;p&gt;실험까지 해보니 S3 Annotations를 볼 때 경계가 조금 더 분명해졌습니다.
S3 Annotations는 객체 옆에 queryable context를 붙이는 기능이지, 검색엔진이나 Graph DB나 권한 모델을 자동 설계해주는 기능은 아닙니다.&lt;/p&gt;

&lt;p&gt;첫째, annotation table은 검색엔진이 아닙니다.
Athena나 Iceberg-compatible engine으로 대량 조회하기에는 좋지만, 즉시 검색 UX, 랭킹, 오타 보정, 의미 기반 검색이 필요하면 OpenSearch나 vector DB를 별도로 봐야 합니다.&lt;/p&gt;

&lt;p&gt;둘째, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;context.relations&lt;/code&gt;는 graph의 시작점이지 Graph DB 자체는 아닙니다.
1-hop, 2-hop 관계를 펼쳐 보는 정도는 Athena에서도 가능하지만, 여러 단계를 따라가며 관계를 탐색하거나 그래프 전체의 중요한 객체를 찾는 용도라면 Neptune 같은 graph DB가 더 맞습니다.&lt;/p&gt;

&lt;p&gt;셋째, annotation table은 비동기입니다.
S3 Metadata table은 backfilling과 refresh 시간이 있으므로, 방금 쓴 annotation이 Athena 조회 결과에 바로 보이지 않을 수 있습니다.
운영 UI에서 “방금 붙인 annotation을 즉시 확인”해야 한다면 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;GetObjectAnnotation&lt;/code&gt; 같은 direct API와 annotation table 조회를 나눠 써야 합니다.&lt;/p&gt;

&lt;p&gt;넷째, annotation은 LLM에게는 신뢰 경계입니다.
Agent가 annotation을 근거로 판단한다면 annotation을 바꾸는 행위는 객체 해석을 바꾸는 행위가 됩니다.
따라서 LLM에는 annotation을 지시문이 아니라 data/tool result로 전달하고, 권한 판단은 IAM, bucket policy, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;governance.*&lt;/code&gt; 같은 검증된 경로에서 다시 확인해야 합니다.&lt;/p&gt;

&lt;p&gt;&lt;br /&gt;&lt;/p&gt;

&lt;h2 id=&quot;outro&quot;&gt;&lt;a href=&quot;#outro&quot;&gt;Outro&lt;/a&gt;&lt;a id=&quot;outro&quot;&gt;&lt;/a&gt;&lt;/h2&gt;

&lt;p&gt;1편에서는 S3 Annotations를 작은 문서 예제로 실험했습니다.
2편에서는 그 범위를 넓혀, S3 객체 전반에 context layer를 붙이는 관점으로 다시 보았습니다.&lt;/p&gt;

&lt;p&gt;핵심은 다음과 같습니다.&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;S3 Metadata tables = 객체 상태와 변경 이력을 queryable table로 제공
S3 Annotations = 객체에 사람이/시스템이 직접 의미 context를 붙이는 기능
Annotation table = 그 context를 Athena에서 대량 조회하는 인덱스
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;그리고 설계의 중심은 namespace입니다.&lt;/p&gt;

&lt;div class=&quot;language-text highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;asset.*        # 객체 자체 설명
analysis.*     # 이미지/오디오/ML 분석
governance.*   # 정책과 보존
agent.*        # Agent 검색/라우팅 단서
context.*      # 객체 간 관계
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;정리하면 S3 Annotations는 단순히 “S3에 metadata를 조금 더 붙이는 기능”이라기보다, 원본 객체는 그대로 두고 객체 옆에 의미 계층을 붙이는 방식에 가깝습니다.
Media Asset 관리에서는 캠페인, 영상, 이미지, 음원, 자막을 같은 맥락으로 묶을 수 있고, AI SRE에서는 incident 문서와 로그, metric, runbook을 연결하는 context layer로 쓸 수 있습니다.&lt;/p&gt;

&lt;p&gt;다만 운영에서는 몇 가지 선을 분명히 그어야 합니다.
namespace는 payload의 책임 기준으로 나누고, 원본 객체 권한과 annotation 권한은 분리해서 봐야 합니다.
대량 탐색은 annotation table이 편하지만 비동기이고, 단일 객체의 최신 context는 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;GetObjectAnnotation&lt;/code&gt;이 더 직접적입니다.
LLM이나 Agent에게 annotation을 넘길 때는 지시문이 아니라 data로 취급하고, 보안 판단은 IAM, bucket policy, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;governance.*&lt;/code&gt; 같은 검증된 경로에서 다시 확인해야 합니다.&lt;/p&gt;

&lt;p&gt;이 선을 지킬 수 있다면 S3 Annotations는 S3 객체를 Agent와 Analytics가 다루기 좋은 의미 단위로 바꾸는 가벼운 context layer가 될 수 있습니다.
다만 이것이 실제 운영 문제에서도 유의미한지는 별도로 확인해야 합니다.
3편에서는 AI SRE 예시로 incident 문서, 로그, metric, runbook을 연결해보고, S3 Annotations 기반 context layer가 Agent의 조사 흐름에 실제로 도움이 되는지 살펴보겠습니다.&lt;/p&gt;

&lt;p&gt;&lt;br /&gt;&lt;/p&gt;

&lt;h2 id=&quot;references&quot;&gt;&lt;a href=&quot;#references&quot;&gt;References&lt;/a&gt;&lt;a id=&quot;references&quot;&gt;&lt;/a&gt;&lt;/h2&gt;

&lt;ul&gt;
  &lt;li&gt;&lt;a href=&quot;https://docs.aws.amazon.com/AmazonS3/latest/userguide/annotations-overview.html&quot;&gt;Amazon S3 Annotations overview&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://docs.aws.amazon.com/AmazonS3/latest/userguide/UsingMetadata.html&quot;&gt;Working with object metadata&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://docs.aws.amazon.com/AmazonS3/latest/userguide/metadata-tables-overview.html&quot;&gt;S3 Metadata tables overview&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://docs.aws.amazon.com/AmazonS3/latest/userguide/metadata-tables-inventory-schema.html&quot;&gt;S3 Metadata live inventory table schema&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://docs.aws.amazon.com/AmazonS3/latest/userguide/metadata-tables-schema.html&quot;&gt;S3 Metadata journal table schema&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://docs.aws.amazon.com/AmazonS3/latest/userguide/metadata-tables-annotation-schema.html&quot;&gt;S3 Metadata annotation table schema&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://docs.aws.amazon.com/AmazonS3/latest/userguide/metadata-tables-example-queries.html&quot;&gt;Example metadata table queries&lt;/a&gt;&lt;/li&gt;
  &lt;li&gt;&lt;a href=&quot;https://aws.amazon.com/blogs/aws/amazon-s3-annotations-attach-rich-queryable-context-directly-to-your-objects/&quot;&gt;Amazon S3 annotations launch blog&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;hr /&gt;

&lt;p&gt;&lt;span class=&quot;table-of-contents-list&quot;&gt;Amazon S3 Annotations&lt;/span&gt;&lt;/p&gt;
&lt;ul class=&quot;table-of-contents-list&quot;&gt;
    &lt;li&gt;&lt;a href=&quot;./S3-OKF-Annotations&quot;&gt;1편: Open Knowledge Format 흉내내기&lt;/a&gt;&lt;/li&gt;
    &lt;li&gt;&lt;a href=&quot;./S3-Annotations-2&quot;&gt;2편: 객체에 의미 계층 붙이기&lt;/a&gt;&lt;/li&gt;
    &lt;li&gt;&lt;a href=&quot;./S3-Annotations-3&quot;&gt;3편: AI SRE를 위한 Observability Context Layer 만들기&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;</content>

      
      
      
      
      

      <author>
          <name>Jihun Lim</name>
        
        
      </author>

      

      
        <category term="aws" />
      
        <category term="genai" />
      
        <category term="lab" />
      

      
        <summary type="html">이번 글에서는 S3 Annotations를 특정 예제의 부가 정보 저장소가 아니라, S3 객체 전반에 의미 계층을 붙이는 context layer로 보는 관점을 정리합니다.</summary>
      

      
      
    </entry>
  
    <entry>
      <title type="html">Amazon S3 Annotations 1편: Open Knowledge Format 흉내내기</title>
      <link href="https://heuristicwave.github.io/S3-OKF-Annotations" rel="alternate" type="text/html" title="Amazon S3 Annotations 1편: Open Knowledge Format 흉내내기" />
      <published>2026-06-24T00:00:00+00:00</published>
      <updated>2026-06-24T00:00:00+00:00</updated>
      <id>https://heuristicwave.github.io/S3-OKF-Annotations</id>
      <content type="html" xml:base="https://heuristicwave.github.io/S3-OKF-Annotations">&lt;p&gt;최근 AWS Blog에 &lt;a href=&quot;https://aws.amazon.com/ko/blogs/korea/amazon-s3-annotations-attach-rich-queryable-context-directly-to-your-objects/&quot;&gt;Amazon S3 annotations 기능 소개&lt;/a&gt; 글이 올라왔습니다.
S3 객체를 다시 쓰지 않고도 JSON, YAML, XML, Text 형식의 설명 정보를 객체 옆에 붙이고, 이를 S3 Metadata annotation table을 통해 Athena에서 조회할 수 있다는 내용입니다.&lt;/p&gt;

&lt;p&gt;비슷한 시점에 Google Cloud Blog에서는 &lt;a href=&quot;https://cloud.google.com/blog/products/data-analytics/how-the-open-knowledge-format-can-improve-data-sharing&quot;&gt;Open Knowledge Format&lt;/a&gt;을 소개했습니다.
OKF는 복잡한 신규 런타임이나 SDK를 전제하지 않고, &lt;strong&gt;Markdown 파일 + YAML frontmatter + Markdown link&lt;/strong&gt;만으로 사람과 Agent가 함께 읽을 수 있는 지식 묶음을 만들자는 접근입니다.&lt;/p&gt;

&lt;p&gt;두 글을 같이 읽다 보니 이런 생각이 들었습니다.&lt;/p&gt;

&lt;blockquote&gt;
  &lt;p&gt;OKF 문서는 S3 객체로 두고, 문서에서 뽑은 구조화 정보는 S3 annotations로 붙이면 되지 않을까?&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;이번 글에서는 이 아이디어를 작게 구현해보겠습니다.&lt;/p&gt;

&lt;blockquote&gt;
  &lt;p&gt;이어지는 글에서는 OKF를 넘어서 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;asset.*&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;vision.*&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;audio.*&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;agent.*&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;governance.*&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;context.relations&lt;/code&gt;처럼 다양한 의미 계층을 S3 객체에 붙이는 방식과 S3 Metadata와의 차이를 다룹니다.&lt;br /&gt;
&lt;a href=&quot;/S3-Annotations-2.html&quot;&gt;Amazon S3 Annotations 2편: 객체에 의미 계층 붙이기&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;

&lt;p&gt;&lt;br /&gt;&lt;/p&gt;

&lt;h2 id=&quot;-okf-문서를-s3-객체로-보기&quot;&gt;&lt;a href=&quot;#concept&quot;&gt;🧠 OKF 문서를 S3 객체로 보기&lt;/a&gt;&lt;a id=&quot;concept&quot;&gt;&lt;/a&gt;&lt;/h2&gt;

&lt;p&gt;OKF의 핵심은 “지식을 파일로 표현한다”는 점입니다.
예를 들어 하나의 데이터셋, 테이블, 메트릭, 플레이북이 각각 Markdown 파일이 됩니다.
파일 상단에는 YAML frontmatter가 있고, 본문에는 사람이 읽을 설명과 다른 문서로 이어지는 링크가 있습니다.&lt;/p&gt;

&lt;div class=&quot;language-markdown highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;nn&quot;&gt;---&lt;/span&gt;
&lt;span class=&quot;na&quot;&gt;type&lt;/span&gt;&lt;span class=&quot;pi&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;Metric&lt;/span&gt;
&lt;span class=&quot;na&quot;&gt;title&lt;/span&gt;&lt;span class=&quot;pi&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;Example Metric&lt;/span&gt;
&lt;span class=&quot;na&quot;&gt;description&lt;/span&gt;&lt;span class=&quot;pi&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;Short description for a metric object.&lt;/span&gt;
&lt;span class=&quot;na&quot;&gt;resource&lt;/span&gt;&lt;span class=&quot;pi&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;athena://analytics/example&lt;/span&gt;
&lt;span class=&quot;na&quot;&gt;tags&lt;/span&gt;&lt;span class=&quot;pi&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;pi&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;metric&lt;/span&gt;&lt;span class=&quot;pi&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;activation&lt;/span&gt;&lt;span class=&quot;pi&quot;&gt;]&lt;/span&gt;
&lt;span class=&quot;na&quot;&gt;timestamp&lt;/span&gt;&lt;span class=&quot;pi&quot;&gt;:&lt;/span&gt; &lt;span class=&quot;s&quot;&gt;2026-06-23T00:00:00Z&lt;/span&gt;
&lt;span class=&quot;nn&quot;&gt;---&lt;/span&gt;

&lt;span class=&quot;gh&quot;&gt;# Example Metric&lt;/span&gt;

This metric is related to &lt;span class=&quot;p&quot;&gt;[&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;source&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;](&lt;/span&gt;&lt;span class=&quot;sx&quot;&gt;source.md&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;.
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;S3에 쌓인 원천 파일, 문서, 이미지, 로그에 의미를 붙이려면 “이 객체가 무엇을 나타내는지”를 별도 DB나 Catalog에 다시 저장하는 경우가 많습니다.
이렇게 되면 S3 객체와 그 객체를 설명하는 정보가 서로 다른 시스템으로 나뉩니다.&lt;/p&gt;

&lt;p&gt;S3 annotations는 이 간극을 줄일 수 있습니다.
객체 본문을 다시 쓰지 않고도, 객체 옆에 구조화된 설명을 annotation으로 붙일 수 있기 때문입니다.
AWS Blog 기준으로 S3 annotation은 객체마다 최대 1,000개를 붙일 수 있고, 각 annotation payload는 최대 1MB입니다.
또한 같은 이름으로 다시 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;PutObjectAnnotation&lt;/code&gt;을 호출하면 객체를 다시 쓰지 않고 annotation만 업데이트할 수 있습니다.
이 정도면 Markdown 원문 전체를 복제하지 않고도 frontmatter, links, summary처럼 검색과 관계 탐색에 필요한 정보만 나눠 담기에 충분합니다.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;/assets/built/images/post/ai/s3-annotations/architecture.svg&quot; alt=&quot;OKF on S3 annotations&quot; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;br /&gt;&lt;/p&gt;

&lt;h2 id=&quot;-markdown-원문과-annotation-나누기&quot;&gt;&lt;a href=&quot;#projection&quot;&gt;🧱 Markdown 원문과 annotation 나누기&lt;/a&gt;&lt;a id=&quot;projection&quot;&gt;&lt;/a&gt;&lt;/h2&gt;

&lt;p&gt;핵심 흐름은 단순합니다.
OKF Markdown 문서는 S3 Object로 올리고, frontmatter, link, summary처럼 검색과 관계 탐색에 필요한 정보는 별도 annotation으로 붙입니다.&lt;/p&gt;

&lt;p&gt;이때 중요한 점은 Markdown 원문을 annotation에 다시 복제하지 않는 것입니다.
원문은 사람이 읽고 수정할 수 있는 Markdown 객체로 유지하고, annotation에는 조회와 필터링에 필요한 작은 구조화 정보만 둡니다.&lt;/p&gt;

&lt;p&gt;예를 들어 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;example.md&lt;/code&gt; 문서라면 다음처럼 나눌 수 있습니다.&lt;/p&gt;

&lt;table&gt;
  &lt;thead&gt;
    &lt;tr&gt;
      &lt;th&gt;항목&lt;/th&gt;
      &lt;th&gt;저장 위치&lt;/th&gt;
      &lt;th&gt;역할&lt;/th&gt;
    &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
    &lt;tr&gt;
      &lt;td&gt;Markdown 본문&lt;/td&gt;
      &lt;td&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf/example.md&lt;/code&gt; S3 Object&lt;/td&gt;
      &lt;td&gt;사람이 읽는 원문과 설명&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;YAML frontmatter&lt;/td&gt;
      &lt;td&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.frontmatter&lt;/code&gt; annotation&lt;/td&gt;
      &lt;td&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;type&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;title&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;tags&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;resource&lt;/code&gt; 같은 queryable field&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;Markdown link&lt;/td&gt;
      &lt;td&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.links&lt;/code&gt; annotation&lt;/td&gt;
      &lt;td&gt;다른 OKF 문서와의 관계&lt;/td&gt;
    &lt;/tr&gt;
    &lt;tr&gt;
      &lt;td&gt;짧은 설명&lt;/td&gt;
      &lt;td&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.summary&lt;/code&gt; annotation&lt;/td&gt;
      &lt;td&gt;검색 결과 미리보기와 Agent retrieval hint&lt;/td&gt;
    &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;

&lt;p&gt;객체 하나를 확인할 때는 object body를 내려받지 않고 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;ListObjectAnnotations&lt;/code&gt;와 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;GetObjectAnnotation&lt;/code&gt;으로 필요한 annotation만 읽을 수 있습니다.
하지만 이 방식만으로는 여러 객체를 한 번에 탐색하기 어렵습니다.
그래서 다음 단계에서는 S3 Metadata annotation table을 통해 여러 객체의 annotation을 Athena에서 조회합니다.&lt;/p&gt;

&lt;p&gt;&lt;br /&gt;&lt;/p&gt;

&lt;h2 id=&quot;-annotation-table에서-후보-객체-찾기&quot;&gt;&lt;a href=&quot;#query&quot;&gt;🔎 Annotation table에서 후보 객체 찾기&lt;/a&gt;&lt;a id=&quot;query&quot;&gt;&lt;/a&gt;&lt;/h2&gt;

&lt;p&gt;객체 하나의 annotation을 읽는 것만으로는 OKF의 장점을 살리기 어렵습니다.
중요한 부분은 여러 객체에 흩어진 정보를 한 번에 찾는 것입니다.&lt;/p&gt;

&lt;p&gt;AWS Blog에 따르면 S3 Metadata annotation table을 활성화하면 annotation이 완전관리형 Apache Iceberg table로 전달되고, Athena 같은 엔진에서 조회할 수 있습니다.
Apache Iceberg는 S3 같은 object storage 위의 데이터를 SQL 엔진이 테이블처럼 읽을 수 있게 해주는 open table format입니다.
예를 들어 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.frontmatter&lt;/code&gt; annotation에서 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;type = Metric&lt;/code&gt;이고 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;tags&lt;/code&gt;에 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;activation&lt;/code&gt;이 들어간 객체를 찾는 쿼리는 다음처럼 생각할 수 있습니다.&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;DISTINCT&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;bucket&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;object_key&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;&quot;s3tablescatalog/aws-s3&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;&quot;b_&amp;lt;bucket_name&amp;gt;&quot;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;.&lt;/span&gt;&lt;span class=&quot;nv&quot;&gt;&quot;annotation&quot;&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;WHERE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;name&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;okf.frontmatter&apos;&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;AND&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;json_extract_scalar&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;text_value&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;$.type&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;Metric&apos;&lt;/span&gt;
  &lt;span class=&quot;k&quot;&gt;AND&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;contains&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;CAST&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;json_extract&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;text_value&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;$.tags&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;array&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;nb&quot;&gt;varchar&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)),&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;activation&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;);&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;이 쿼리의 핵심은 객체 본문이 아니라 annotation table을 먼저 본다는 점입니다.
Markdown 객체를 내려받아 frontmatter를 매번 parsing하지 않고, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.frontmatter&lt;/code&gt; annotation으로 분리해둔 field만으로 후보 객체를 좁힙니다.
이후 Agent나 애플리케이션은 필요한 객체만 선택적으로 읽으면 됩니다.
이 흐름은 RAG의 “무조건 chunk 검색”과는 조금 다릅니다.
문서의 의미와 관계를 명시적으로 붙여두고, 그 구조화된 정보로 retrieval 후보를 줄이는 방식입니다.&lt;/p&gt;

&lt;p&gt;&lt;br /&gt;&lt;/p&gt;

&lt;h2 id=&quot;️-okf-context-inspector로-확인하기&quot;&gt;&lt;a href=&quot;#inspector&quot;&gt;🖥️ OKF Context Inspector로 확인하기&lt;/a&gt;&lt;a id=&quot;inspector&quot;&gt;&lt;/a&gt;&lt;/h2&gt;

&lt;p&gt;동작을 확인하기 위해 간단한 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;OKF Context Inspector&lt;/code&gt;도 만들어보았습니다.
왼쪽 패널에서 검색어와 OKF type, domain, tag를 입력하면 백엔드가 Athena로 S3 Metadata annotation table을 조회합니다.
실행된 API 요청과 Athena query execution id도 함께 노출해두어, 브라우저 안에서 단순히 JSON을 필터링하는 것이 아니라 실제 annotation table 조회 흐름을 확인할 수 있게 했습니다.&lt;/p&gt;

&lt;p&gt;아래 화면은 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rag&lt;/code&gt; 키워드로 검색한 결과입니다.
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.frontmatter&lt;/code&gt; annotation에 들어 있는 title, description, tag를 기준으로 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;RAG Evaluation Set&lt;/code&gt; 객체를 찾습니다.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;/assets/built/images/post/ai/s3-annotations/okf-context-inspector-search.png&quot; alt=&quot;OKF Context Inspector search result&quot; /&gt;
&lt;em&gt;&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;rag&lt;/code&gt; 검색 결과. 왼쪽에는 Athena 실행 상태와 실제 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;/api/search&lt;/code&gt; 요청이 보이고, 가운데에는 annotation table 조회 결과로 좁혀진 S3 object가 표시됩니다.&lt;/em&gt;&lt;/p&gt;

&lt;p&gt;예를 들어 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;benchmark&lt;/code&gt; 같은 키워드로 검색하면 백엔드는 다음과 같은 Athena SQL을 실행합니다.
검색 대상은 객체 본문이 아니라 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.frontmatter&lt;/code&gt; annotation의 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;text_value&lt;/code&gt;입니다.&lt;/p&gt;

&lt;div class=&quot;language-sql highlighter-rouge&quot;&gt;&lt;div class=&quot;highlight&quot;&gt;&lt;pre class=&quot;highlight&quot;&gt;&lt;code&gt;&lt;span class=&quot;k&quot;&gt;SELECT&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;object_key&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;json_extract_scalar&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;text_value&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;$.title&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;title&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;json_extract_scalar&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;text_value&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;$.type&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;okf_type&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;json_extract_scalar&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;text_value&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;$.domain&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;domain&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;json_format&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;json_extract&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;text_value&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;$.tags&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;))&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AS&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;tags&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;,&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;text_value&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;FROM&lt;/span&gt; &lt;span class=&quot;nv&quot;&gt;&quot;annotation&quot;&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;WHERE&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;name&lt;/span&gt; &lt;span class=&quot;o&quot;&gt;=&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;okf.frontmatter&apos;&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;AND&lt;/span&gt;
  &lt;span class=&quot;n&quot;&gt;regexp_like&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;k&quot;&gt;lower&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;(&lt;/span&gt;&lt;span class=&quot;n&quot;&gt;text_value&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;),&lt;/span&gt; &lt;span class=&quot;s1&quot;&gt;&apos;(^|[^A-Za-z0-9])benchmark($|[^A-Za-z0-9])&apos;&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;)&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;ORDER&lt;/span&gt; &lt;span class=&quot;k&quot;&gt;BY&lt;/span&gt; &lt;span class=&quot;n&quot;&gt;object_key&lt;/span&gt;
&lt;span class=&quot;k&quot;&gt;LIMIT&lt;/span&gt; &lt;span class=&quot;mi&quot;&gt;100&lt;/span&gt;&lt;span class=&quot;p&quot;&gt;;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;객체를 선택하면 오른쪽에서 해당 객체에 붙은 annotation 내용을 확인할 수 있습니다.
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.frontmatter&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.links&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.summary&lt;/code&gt;를 분리해 보여주고, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.links&lt;/code&gt;를 기준으로 어떤 문서가 이 객체를 참조하고 어떤 문서를 참조하는지도 간단한 Link Graph로 표현했습니다.
여기서 주목할 부분은 기존처럼 특정 S3 prefix 아래를 탐색하는 방식과 다르다는 점입니다.
&lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf/generated/ai/...&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf/metrics/...&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf/index.md&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf/generated/technology/...&lt;/code&gt;처럼 서로 다른 목적의 객체가 각기 다른 prefix에 있더라도, annotation에 들어 있는 link 정보를 기준으로 연결 관계를 따라갈 수 있습니다.&lt;/p&gt;

&lt;p&gt;&lt;img src=&quot;/assets/built/images/post/ai/s3-annotations/okf-context-inspector-payload-graph.png&quot; alt=&quot;OKF Context Inspector annotation payload and link graph&quot; /&gt;
&lt;em&gt;선택한 S3 object의 annotation payload와 link graph. 참조 대상 객체들이 서로 다른 S3 prefix에 있어도, 객체에 붙은 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.links&lt;/code&gt; annotation을 기준으로 연결된 문서를 찾을 수 있습니다.&lt;/em&gt;&lt;/p&gt;

&lt;p&gt;이 데모의 목적은 예쁜 UI를 만드는 것이 아니라, S3 Annotations가 단순히 “객체에 메모를 붙이는 기능”을 넘어 조회 가능한 컨텍스트 계층으로 동작할 수 있음을 보여주는 것입니다.
객체 본문은 S3에 그대로 두고, 검색과 관계 탐색에 필요한 작은 정보만 annotation으로 분리하면 Agent나 분석 도구가 먼저 참고할 수 있는 얇은 인덱스를 만들 수 있습니다.&lt;/p&gt;

&lt;p&gt;&lt;br /&gt;&lt;/p&gt;

&lt;h2 id=&quot;-실험을-통해-든-생각&quot;&gt;&lt;a href=&quot;#design&quot;&gt;🧩 실험을 통해 든 생각&lt;/a&gt;&lt;a id=&quot;design&quot;&gt;&lt;/a&gt;&lt;/h2&gt;

&lt;p&gt;첫 번째로, annotation을 붙여보면 S3 객체가 두 계층으로 나뉩니다.
Markdown 객체는 사람이 읽고 수정하는 원문이고, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.frontmatter&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.links&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.summary&lt;/code&gt; annotation은 Athena와 Agent가 먼저 조회하는 얇은 인덱스입니다.&lt;/p&gt;

&lt;p&gt;다만 annotation은 원문에서 분리된 조회용 사본이므로, 원문과 annotation 사이의 정합성을 관리해야 합니다.
Markdown 본문이나 frontmatter가 바뀌었는데 annotation이 갱신되지 않으면, Athena는 오래된 정보를 기준으로 후보 객체를 찾습니다.
따라서 실험 수준에서는 수동으로 붙일 수 있지만, &lt;strong&gt;실제 운영에서는 객체 변경을 감지해 annotation을 다시 생성하는 pipeline이 필요합니다.&lt;/strong&gt;&lt;/p&gt;

&lt;p&gt;두 번째로, 이 글의 범위에서는 S3 Annotations를 “OKF를 S3 위에서 표현해보는 구현 방법” 정도로만 다룹니다.
객체 본문 권한과 annotation 권한을 어떻게 나눌지, Agent가 annotation을 얼마나 신뢰해도 되는지, annotation namespace를 누가 관리할지는 별도의 설계 문제입니다.
이 부분은 &lt;a href=&quot;/S3-Annotations-2.html&quot;&gt;2편&lt;/a&gt;에서 S3 Annotations 자체를 객체 컨텍스트 계층으로 놓고 다시 정리해보겠습니다.&lt;/p&gt;

&lt;p&gt;마지막으로, 1편의 namespace는 의도적으로 좁게 잡았습니다.
이번 데모에서는 단순히 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.frontmatter&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.links&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.summary&lt;/code&gt; 세 가지로 나눴습니다.
이 세 이름은 OKF Markdown 문서의 구조를 S3 annotation으로 옮긴 것일 뿐입니다.
이미지, 오디오, PDF, Parquet, 로그처럼 Markdown 문서가 아닌 객체까지 다루려면 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.*&lt;/code&gt;만으로는 부족합니다.
그 확장 설계는 2편에서 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;asset.*&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;vision.*&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;audio.*&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;agent.*&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;governance.*&lt;/code&gt;, &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;context.relations&lt;/code&gt; 같은 의미 계층으로 다시 다뤄보겠습니다.&lt;/p&gt;

&lt;p&gt;&lt;br /&gt;&lt;/p&gt;

&lt;h2 id=&quot;마무리&quot;&gt;&lt;a href=&quot;#outro&quot;&gt;마무리&lt;/a&gt;&lt;a id=&quot;outro&quot;&gt;&lt;/a&gt;&lt;/h2&gt;

&lt;p&gt;OKF가 말하는 방향은 “Agent가 읽을 수 있는 지식을 복잡한 플랫폼이 아니라 간단한 파일 규약으로 교환하자”에 가깝습니다.
S3 annotations는 이 아이디어를 S3 위에서 실험해볼 수 있는 구현 방법 중 하나입니다.
OKF만을 위한 전용 인프라는 아니지만, 객체와 설명 정보를 함께 보관하고 annotation table로 조회해야 하는 환경에서는 꽤 좋은 접점이 될 수 있어 보입니다.&lt;/p&gt;

&lt;p&gt;물론 이 모델이 검색엔진을 대체하는 것은 아닙니다.
annotation table은 객체를 즉시 전문 검색하는 장치라기보다, 구조화된 정보로 후보 객체를 좁히는 조회 계층에 가깝습니다.
실제 운영에서는 S3 Metadata, S3 Tables, Athena 권한, annotation table 반영 지연, namespace 관리까지 함께 설계해야 합니다.
그래도 “S3 객체에 원문을 두고, Agent가 읽을 수 있는 설명 정보를 같은 객체에 붙인다”는 모델은 단순하고 직관적입니다.&lt;/p&gt;

&lt;p&gt;이미 S3에 문서나 데이터셋이 쌓여 있고, 그 위에 Agent가 참고할 설명과 관계 정보를 붙이고 싶다면 한 번쯤 실험해볼 만한 패턴이라고 생각합니다.&lt;/p&gt;

&lt;p&gt;다만 이 패턴을 OKF 문서 밖으로 확장하면 이야기가 달라집니다.
이미지, 오디오, PDF, 데이터셋 같은 객체까지 다루려면 &lt;code class=&quot;language-plaintext highlighter-rouge&quot;&gt;okf.*&lt;/code&gt;가 아니라 더 넓은 annotation namespace와 객체 간 relation 모델이 필요합니다.
이 내용은 &lt;a href=&quot;/S3-Annotations-2.html&quot;&gt;Amazon S3 Annotations 2편: 객체에 의미 계층 붙이기&lt;/a&gt;에서 이어서 다룹니다.&lt;/p&gt;

&lt;p&gt;잘못된 내용은 지적해주세요! 😃&lt;/p&gt;

&lt;hr /&gt;

&lt;p&gt;&lt;span class=&quot;table-of-contents-list&quot;&gt;Amazon S3 Annotations&lt;/span&gt;&lt;/p&gt;
&lt;ul class=&quot;table-of-contents-list&quot;&gt;
    &lt;li&gt;&lt;a href=&quot;./S3-OKF-Annotations&quot;&gt;1편: Open Knowledge Format 흉내내기&lt;/a&gt;&lt;/li&gt;
    &lt;li&gt;&lt;a href=&quot;./S3-Annotations-2&quot;&gt;2편: 객체에 의미 계층 붙이기&lt;/a&gt;&lt;/li&gt;
    &lt;li&gt;&lt;a href=&quot;./S3-Annotations-3&quot;&gt;3편: AI SRE를 위한 Observability Context Layer 만들기&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;</content>

      
      
      
      
      

      <author>
          <name>Jihun Lim</name>
        
        
      </author>

      

      
        <category term="aws" />
      
        <category term="genai" />
      
        <category term="lab" />
      

      
        <summary type="html">최근 AWS Blog에 Amazon S3 annotations 기능 소개 글이 올라왔습니다. S3 객체를 다시 쓰지 않고도 JSON, YAML, XML, Text 형식의 설명 정보를 객체 옆에 붙이고, 이를 S3 Metadata annotation table을 통해 Athena에서 조회할 수 있다는 내용입니다.</summary>
      

      
      
    </entry>
  
</feed>
